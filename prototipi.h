#include <string>
using namespace std;
//funzione che trascrive una qualunque stringa sul file di testo;
void scriviTesto(string stringa);
//funzione che legge riga per riga il contenuto del file, lo inserisce alla stringa, e la stampa fino alla fine del file;
void leggiTesto(string stringa);